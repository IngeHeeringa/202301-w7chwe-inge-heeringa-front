import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ba000d",
    },
    secondary: {
      main: "#3a3a3a",
    },
  },
});

export default theme;
